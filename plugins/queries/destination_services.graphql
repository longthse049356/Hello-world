query($param: ServiceSearchParam!) {
    destination_services(param: $param) {
        edges {
            id
            title
            service_kind
            photos{
                title
                thumbnails
            }
        }
        page
        limit
        total_count
    }
}
